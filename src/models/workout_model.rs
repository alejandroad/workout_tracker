pub struct WorkoutEntry {
    pub exercise_name: String,
    pub weight: u64,
    pub sets: u64,
    pub reps: u64,
    pub perceived_effort: u64,
    pub date: String,
    pub description: String,
}
